# JARVIS Status

## Current state

- WSL environment cleaned and ready.
- Codex CLI and OMX installed and smoke-tested.
- Codex OAuth/ChatGPT Pro Personal workspace verified previously.
- Claude Code and OMC installed and smoke-tested as secondary external executors.
- Claude Code OAuth/Claude Max account verified for external CLI use.
- Bubblewrap installed and Codex sandbox warning removed.
- Hermes approval policy set to smart.
- Secret redaction enabled for future sessions.
- JARVIS control-plane root created at `/home/hskim/jarvis`.
- Project root created at `/home/hskim/projects`.

## Active operating model

- Hermes is the control-plane/orchestrator/verifier/memory manager.
- Codex CLI + OMX remain the default medium/large implementation executor line.
- Claude Code + OMC are available as a secondary external executor line for Claude-strength review, planning, refactoring, and quota/load balancing.
- Codex `/goal` is available for repo-local iterative cleanup when useful.
- Harness documents define automatic routing and completion rules.
- just-chill vNext is the current operating-layer direction: development requests route to GJC, while just-chill owns intake, risk/routing, result summaries, and memory promotion policy.
- The first executable just-chill slice is `scripts/just_chill_router.py`, with acceptance checks in `scripts/check_just_chill_router.py`.
- The second executable just-chill slice is contract-level bridge/memory planning: `scripts/just_chill_bridge.py`, `scripts/just_chill_memory_contracts.py`, and `scripts/check_just_chill_bridge_contracts.py`.
- The third executable just-chill slice maps live GJC/Hermes boundaries: `scripts/just_chill_live_bindings.py`, `scripts/just_chill_hermes_adapter.py`, `scripts/check_just_chill_live_bindings.py`, and `harnesses/just-chill-live-bindings.md`.
- The fourth executable just-chill slice adds repo-local host-owned visible-session helpers: `scripts/just_chill_visible_session_helpers.py`, `scripts/create-gjc-session`, `scripts/prompt-gjc-session`, `scripts/tail-gjc-session`, and `scripts/check_just_chill_visible_helpers.py`.
- The fifth executable just-chill slice extends those helpers with dry-run `tmux-orchestration-plan-v1` argv plans and fail-closed live readiness when `tmux`/`gjc` or durable evidence requirements are missing.
- The sixth executable just-chill slice adds Hermes live-boundary reports: `scripts/just_chill_hermes_adapter.py` now distinguishes read-only Hermes status/MCP/setup visibility from raw artifact writes, and maps active Holographic provider tools as host-owned summary/fact-memory write plans when available.
- The seventh executable just-chill slice adds deterministic ontology contracts: `scripts/just_chill_ontology_contracts.py`, `scripts/check_just_chill_ontology_contracts.py`, and `harnesses/just-chill-ontology-contracts.md` define contract-level TBox/ABox candidates and SHACL-style promotion blockers without live RDF/Hermes writes.
- The eighth executable just-chill slice extends ontology contracts with deterministic RDF/OWL Turtle export manifests and SHACL shape/validation-report exports, still without live RDF store writes or live SHACL engine claims.
- The ninth executable just-chill slice adds local raw artifact staging: `scripts/just_chill_raw_artifact_store.py` writes ignored repo-local content/contract/write/delete receipts, blocks sensitive reads without approval, and preserves Hermes as canonical storage authority until a real Hermes raw artifact API is mapped.
- The tenth executable just-chill slice adds provider-backed summary memory receipts: `scripts/just_chill_summary_memory_receipts.py` emits host-owned Holographic `fact_store(add/remove)` receipt plans, records local add/remove receipts from supplied provider evidence, requires sensitive/destructive approval, and keeps Hermes as canonical memory authority.
- The eleventh executable just-chill slice adds Hermes raw artifact API discovery and promotion planning: `scripts/just_chill_hermes_raw_artifact_boundary.py` probes read-only Hermes/GJC surfaces for raw artifact create/read/delete APIs or MCP tools, exposes fail-closed `rawArtifactApiDiscovery` in the Hermes adapter, and emits host-owned local-staging-to-Hermes promotion plans without calling Hermes.
- The twelfth executable just-chill slice adds RDF/SHACL live-boundary persistence planning: `scripts/just_chill_ontology_contracts.py` reports host RDF parser / SHACL engine / Hermes RDF graph API readiness and emits host-owned persistence plans without local SHACL/Hermes execution.
- The thirteenth executable just-chill slice prepares the external live Hermes API: `scripts/just_chill_hermes_memory_mcp.py` is a host-owned stdio MCP server registered as `just_chill_memory_api`, exposing raw artifact, RDF graph, vector sidecar, and status tools with hash, approval, delete, read-back, and retrieval receipt guards.
- The fourteenth executable just-chill slice adds host-owned live receipt runners: `scripts/just_chill_hermes_mcp_receipts.py` exercises raw artifact, RDF graph, and vector sidecar MCP lifecycles with read-back/search/delete evidence, while `scripts/just_chill_rdf_persistence_receipts.py` combines live `pyshacl` evidence with Hermes RDF graph receipts without making just-chill the executor.
- The fifteenth executable just-chill slice adds vector sidecar and recall-gate contracts: `scripts/just_chill_vector_recall.py` maps provider search vs vector authority, creates non-canonical sidecar candidates from Hermes-canonical refs/hashes, and gates recall admission with access, deletion, redaction, freshness, provenance, and durable retrieval evidence checks.
- The sixteenth executable just-chill slice extends the host-owned `just_chill_memory_api` MCP server with vector sidecar create/search/read/delete tools. Vector sidecars remain non-canonical: they reference Hermes-owned source ids/hashes, block deleted/redacted/sensitive sources without approval, and provide durable host retrieval evidence for the recall gate.
- The seventeenth executable just-chill slice adds a non-sensitive memory migration fixture replay: `scripts/just_chill_memory_migration_fixture.py` selects repository wiki design facts, exercises host-owned Hermes raw/RDF/vector MCP lifecycles plus summary receipts in a temporary store, and cleans up without private memory promotion.
- The eighteenth executable just-chill slice adds a safe debug/fixture CLI contract entrypoint: `scripts/just_chill_cli.py` and executable wrapper `scripts/just-chill` produce JSON for `route`, `remember`, `recall`, and `handoff-gjc` flows without executing GJC, writing Hermes, or claiming canonical memory authority. The CLI is not the product UX; Hermes is the user-facing layer.
- The nineteenth executable just-chill slice encodes coordinator/delegation mutation consent policy: `scripts/just_chill_gjc_consent_policy.py` and `scripts/check_just_chill_gjc_consent_policy.py` keep visible sessions first and require coordinator smoke, mutation classes, per-call `allow_mutation`, delegate availability, durable evidence, and scrollback rejection before host mutation is ready.
- The twentieth executable just-chill slice adds an end-to-end dogfood contract harness: `scripts/just_chill_dogfood_harness.py` and `scripts/check_just_chill_dogfood_harness.py` exercise route -> GJC handoff -> memory/raw/RDF/SHACL/vector contracts -> recall gate -> consent policy without hidden execution.
- The twenty-first executable just-chill slice adds the Hermes-facing harness adapter and MCP wrapper: `scripts/just_chill_harness.py` exposes route/remember/recall/GJC-handoff/consent/status/handle contracts for Hermes callers, and `scripts/just_chill_harness_mcp.py` exposes matching `just_chill.*` stdio MCP tools registered in Hermes as `just_chill_harness` after explicit operator approval.
- The twenty-second executable just-chill slice adds a Hermes-main dogfood harness: `scripts/just_chill_hermes_harness.py` and `scripts/check_just_chill_hermes_harness.py` prove Hermes remains the user-facing entrypoint while just-chill acts as a harness dependency and CLI remains debug/test-only.
- The twenty-third executable just-chill slice adds a host-owned approval registry: `scripts/just_chill_approval_registry.py` issues, verifies, and revokes scope/subject/expiry-bound approval tokens while storing only token hashes; `scripts/just_chill_cli.py`, `scripts/just_chill_harness.py`, and the registered MCP accept registry-backed sensitive memory/recall approval when `--approval-registry`, `approvalRegistry`, or `JUST_CHILL_APPROVAL_REGISTRY` is supplied.
- The twenty-fourth executable just-chill slice adds a host-owned visible GJC execution bridge MVP: `scripts/just_chill_gjc_execution_bridge.py` consumes `gjcHandoffPlan`, prepares task/session artifacts and operator argv plans, and verifies durable completion evidence while still refusing hidden GJC starts, prompt injection, coordinator/delegate calls, Hermes writes, and scrollback completion.

## Recent changes

- 2026-05-06: Created user-local JARVIS Hermes skills:
  - `jarvis-core`
  - `jarvis-executor-router`
  - `jarvis-codex-omx-executor`
  - `jarvis-wiki-manager`
- 2026-05-07: Initialized ontology-informed LLM Wiki structure under `wiki/` with `SCHEMA.md`, `index.md`, `log.md`, entity/concept/comparison pages, and skill catalog/lifecycle pages.
- 2026-05-11: Reviewed current JARVIS progress with the user and confirmed the control-plane repo had been pushed to GitHub with local `main` synchronized to `origin/main`.
- 2026-05-11: Clarified the ontology-informed wiki model: pages act as graph nodes, YAML frontmatter acts as node metadata/properties, typed `relations` act as graph edges, and `SCHEMA.md` defines the allowed vocabulary.
- 2026-05-11: Recorded the rationale for using markdown-native lightweight ontology now instead of full RDF/OWL: lower operational overhead, better human/agent editability, git-friendly diffs, flexible schema evolution, and possible future RDF/JSON-LD export.
- 2026-05-11: Added `hermes-background` to the routing pipeline for long research/analysis/review work, documented that synchronous `delegate_task` must not be treated as durable background execution, and added an acceptance test matrix plus `scripts/check_executor_routing_policy.py`.
- 2026-05-11: Ran JARVIS routing tests: 8-case conversational routing rehearsal passed, durable background smoke test completed via `terminal(background=true, notify_on_complete=true)`, and no lingering background processes or cron jobs remained afterward.
- 2026-05-11: Created `/home/hskim/projects/jarvis-executor-smoke` as a WSL-native git smoke repo, registered it in `config/projects.yaml`, verified `codex-exec` with a README-only change, and verified `omx-ralph` style execution with a subtract CLI feature plus unittest coverage.
- 2026-05-12: Recovered Claude Code OAuth login for the user's Claude Max account, verified `claude -p` print mode with `CLAUDE-CODE-OK`, installed OMC package `oh-my-claude-sisyphus` 4.13.7 under the user npm prefix, and ran `omc setup` to sync Claude Code agents/skills/hooks.
- 2026-05-14: Implemented background executor hygiene standards after `hermes-slide-director` Phase 13: long Producer/Reviewer work now prefers durable background execution, executor prompts/logs are standardized under ignored `tmp/executor-*` paths, `.omx/` is documented as ignored runtime state, and harnesses warn against long prompts in process argv.
- 2026-06-23: Reflected the just-chill vNext design in the wiki: `concepts/just-chill-vnext-operating-layer.md` is canonical, older Jarvis vNext pages remain predecessor context, and the Hermes/GJC bridge reference is now the integration contract.
- 2026-06-24: Implemented the first just-chill executable slice: a deterministic request-to-route handoff packet generator in `scripts/just_chill_router.py`, a focused acceptance check in `scripts/check_just_chill_router.py`, and the harness contract in `harnesses/just-chill-router.md`.
- 2026-06-24: Added the second just-chill migration slice: `wiki/projects/jarvis/just-chill-migration-inventory.md` classifies current assets, `scripts/just_chill_bridge.py` emits non-executing GJC bridge plans, `scripts/just_chill_memory_contracts.py` emits Hermes artifact/memory contract skeletons, and `harnesses/just-chill-bridge-contracts.md` documents the contract.
- 2026-06-24: Added the third just-chill live-binding slice: `scripts/just_chill_live_bindings.py` probes local GJC/Hermes surfaces and emits fail-closed visible-session handoff instructions, `scripts/just_chill_hermes_adapter.py` wraps memory contracts in a Hermes-owned write-blocked adapter stub, and `harnesses/just-chill-live-bindings.md` records the current host map.
- 2026-06-24: Added the fourth just-chill host-helper slice: repo-local `create-gjc-session`, `prompt-gjc-session`, and `tail-gjc-session` helpers now expose JSON contracts, record visible-session metadata, reject scrollback-only completion, and make the visible-session bridge ready when contracts are probed cleanly.
- 2026-06-24: Added the fifth just-chill visible-orchestration planning slice: visible-session helpers now support `--tmux-plan`, live binding readiness reports `orchestration-plan-ready` only when helper contracts plus `tmux`/`gjc` are available, and checks cover invalid targets, missing tooling, no hidden execution, and durable evidence gates.
- 2026-06-24: Added the sixth just-chill Hermes live-boundary slice: the Hermes adapter now emits `just-chill-hermes-live-boundary-v1` reports, preserves Hermes storage authority, blocks sensitive memory without approval, treats status/MCP/setup probes as read-only visibility, and keeps raw artifact writes fail-closed until a real API is mapped.
- 2026-06-24: Added the seventh just-chill ontology contract slice: deterministic TBox classes/properties, ABox promotion candidates, source provenance, assertion-kind promotion policy, and SHACL-style blockers now cover Decision/Policy explicit confirmation, PreferenceAssertion auto-promotion requirements, sensitive sources, deleted/redacted sources, and unmapped Hermes boundaries.
- 2026-06-24: Added the eighth just-chill RDF/SHACL export slice: ontology contracts now emit stable Turtle text, source contract hashes, provenance/promotion-policy triples, deterministic SHACL shapes, and validation reports while preserving contract-only live-binding status and blocking fake persistence receipts.
- 2026-06-24: Mapped the active Hermes `holographic` provider as a host-owned summary/fact-memory surface: just-chill can now emit a deterministic `fact_store(action=add)` write plan for summary memory while keeping raw artifact storage unmapped and local execution disabled.
- 2026-06-24: Added the ninth just-chill local raw artifact staging slice: raw artifact contracts can now be staged under ignored repo-local storage with content-hash validation, sensitive staging/read approval blocking, write receipts, deletion receipts, invalid-id rejection, and Hermes adapter exposure without claiming canonical Hermes storage.
- 2026-06-24: Added the tenth just-chill summary memory receipt slice: provider-backed summary memory plans now cover Holographic `fact_store(add/remove)` operations, local add/remove receipt paths, sensitive write approval, removal approval/reason/prior-add gates, durable provider result evidence, and Hermes adapter exposure without local provider execution.
- 2026-06-24: Added the eleventh just-chill raw artifact API discovery slice: read-only probes now check Hermes help, memory help, MCP list, tools list, and GJC Hermes setup smoke output, but only MCP/tools listings are accepted as authoritative raw artifact create/read/delete candidates; current local state remains unmapped/fail-closed, and promotion plans require local staging receipts, mapped create/read/delete APIs, sensitive approval, and future read-back hash evidence.
- 2026-06-24: Added the twelfth just-chill RDF/SHACL live-boundary slice: local `rdflib`/`rdfpipe` parsing support was visible, `pyshacl` and Hermes RDF graph create/read/delete APIs were not yet mapped, and RDF/ABox persistence plans remained host-owned, fail-closed, and evidence-gated.
- 2026-06-24: Added and registered the thirteenth just-chill Hermes memory MCP API slice: `hermes mcp add just_chill_memory_api ...` connected, enabled the host-owned raw artifact, RDF graph, vector sidecar, and status tools, and points canonical host-owned storage at `/home/hskim/.local/share/jarvis/just-chill-hermes-memory-api`.
- 2026-06-25: Added host-owned live receipt runners for the mapped Hermes APIs: MCP lifecycle receipts now prove raw/RDF/vector create-read/search-delete behavior, live `pyshacl` is installed/mapped, and RDF persistence receipts can produce conforming SHACL evidence plus Hermes RDF read-back/delete receipts for eligible fixture candidates.
- 2026-06-25: Added vector sidecar / recall-gate contracts: Holographic provider search is mapped as summary search rather than vector authority, and recall candidates require canonical Hermes refs, fresh hashes, scope checks, deletion/redaction checks, and durable host retrieval evidence before entering context.
- 2026-06-25: Extended `just_chill_memory_api` with host-owned `hermes.vector_sidecar.create/search/read/delete` tools and receipt coverage. The vector live-boundary now reports `live-vector-api-mapped` when the MCP server is configured, while just-chill still does not embed text or execute vector search itself.
- 2026-06-25: Added the non-sensitive memory migration fixture replay: selected just-chill design facts from the canonical wiki are replayed through host-owned raw/RDF/vector lifecycle receipts plus summary receipts, preserving provenance, read-back hashes, retention/access/deletion fields, and no private memory promotion.
- 2026-06-25: Added the safe just-chill CLI contract entrypoint: `scripts/just-chill` supports route, remember, recall, and GJC handoff contract output with authority-boundary flags proving no local GJC execution, Hermes writes, or memory ownership; recall stays availability-only unless `--probe` is explicitly supplied and malformed/fake-approval inputs fail closed.
- 2026-06-25: Added the GJC coordinator/delegation consent policy slice: coordinator/delegate paths now have deterministic fail-closed checks for mutation classes, per-call consent, durable evidence, delegate availability, and scrollback rejection.
- 2026-06-25: Added the end-to-end dogfood contract harness, proving the integrated just-chill contract chain passes locally and fail-closes on stale, deleted, or redacted recall evidence and sensitive memory.
- 2026-06-26: Added the Hermes-facing just-chill harness adapter and MCP wrapper: route/remember/recall/GJC-handoff/consent/status/handle are now available as importable contracts and stdio MCP tools for Hermes callers, with all execution/write/search authority disabled; external Hermes MCP registration remained approval-gated until the later explicit approval entry below.
- 2026-06-26: Added the Hermes-main dogfood harness proving the intended product flow: Hermes receives user requests, calls just-chill as a policy harness, and receives host-owned next steps for GJC, memory, or external tool routing while `scripts/just-chill` remains debug/test/fixture-only.
- 2026-06-26: Registered the Hermes-facing just-chill harness MCP after explicit approval: `hermes mcp add just_chill_harness --command python3 --args /home/hskim/jarvis/scripts/just_chill_harness_mcp.py` connected, enabled all 7 `just_chill.*` tools, and `hermes mcp test just_chill_harness` verified connection/tool discovery.
- 2026-06-26: Added the just-chill approval registry: host-owned approval tokens are now hash-persisted, scope/subject/expiry/revocation-verified, and can gate sensitive remember/recall flows through CLI, harness, and MCP arguments.
- 2026-06-26: Fresh Hermes session tool-call evidence confirmed `just_chill.status: ready`: `just_chill_harness` and `just_chill_memory_api` are enabled, GJC/Hermes/tmux commands are visible, coordinator MCP smoke is ok, `gjc_delegate_plan`/`gjc_delegate_execute`/`gjc_delegate_team` are present, visible routed-session helpers are contract-valid, and just-chill still reports no local execution/Hermes-write/mutation authority.
- 2026-06-26: Added the visible GJC execution bridge MVP: `scripts/just_chill_gjc_execution_bridge.py` prepares host-owned visible-session task/session metadata from a `gjcHandoffPlan` and accepts completion only from durable evidence; `scripts/check_just_chill_gjc_execution_bridge.py` covers non-visible bridge blocking and scrollback rejection.

## Next steps

1. Start new Hermes sessions after future MCP registration changes; the current fresh session has verified `just_chill.status: ready`.
2. Use `scripts/just_chill_approval_registry.py` or a future Hermes-native approval registry for production sensitive memory/recall approvals; shape-only tokens are debug/fixture-only.
3. Use `scripts/just_chill_gjc_execution_bridge.py` as the current visible-session handoff bridge; any production automation that actually runs tmux/GJC must remain host-owned and return durable evidence.
4. Use the consent policy before enabling coordinator/delegation execution: configure mutation classes deliberately, pass per-call `allow_mutation`, and preserve durable evidence.
5. For real memory migration, choose approved source artifacts/candidates and replay them through the host-owned fixture/receipt runners, preserving read-back hash evidence and explicit approval for anything personal or sensitive.
6. Bind local summary add/remove receipts to a Hermes-native receipt/audit API if one becomes available.
7. Add production semantic embedding/index runners only after an explicit operator-owned model/index policy is chosen; the mapped vector sidecar API currently stores/searches host-owned sidecar metadata by exact text hash or canonical source id.
